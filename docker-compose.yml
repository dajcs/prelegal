services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - prelegal_data:/data
    env_file:
      - .env
    environment:
      - DB_PATH=/data/prelegal.db

volumes:
  prelegal_data:
